server:
  port: 8888
management:
  server:
    ssl:
      enabled: false  
spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URL}
          timeout: 120
          clone-on-start: true
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}